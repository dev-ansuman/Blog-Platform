services:
  api:
    image: blog-image
    build: .
    expose: 
      - "3000"
    env_file:
      - .env
    
  nginx: 
    image: nginx:alpine
    ports: 
      - "80:80"
    volumes: 
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro  
          # ./nginx/nginx.conf:/etc/nginx/nginc.conf:ro 
    depends_on:
      - api